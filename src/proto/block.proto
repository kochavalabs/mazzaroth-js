syntax = "proto3";

package pb;

message Block {
    // Order is preserved in repeated fields   
    repeated string      transaction_hashes  = 1;
    repeated string     receipts            = 2;
    repeated string      events              = 3;
    string              transaction_db      = 4;
}
